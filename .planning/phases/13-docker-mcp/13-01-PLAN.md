# Plan 13-01 — Explicit MCP Exposure + Persisted Desired Listen

## Tasks

1. Add an explicit Host/Control Plane API for non-loopback MCP listen while leaving existing StartHTTP/StartMCP safe defaults unchanged.
2. Upgrade desired runtime persistence to a backwards-compatible v2 schema containing Workspace ID + listen address; read v1 as loopback defaults.
3. Extend RuntimeOwner and daemon runtime control request with start options (`listen`, explicit exposure), reconcile using persisted listen intent, and expose derived local/docker client endpoints in RuntimeStatus.
4. Extend `runtime start` with `--docker` and `--listen`; default remains loopback.
5. Add regression/security/restart tests and run fmt/test/vet/build.

## Exit

After verification, mark USE-01 validated and auto-advance to Phase 14. Do not start v0.4.
