# Plan 14-01 — up/down/ps/ctl Usability Vertical Slice

## Tasks

1. Add target resolution helper: Workspace ID or canonical directory path; omitted target defaults cwd; `up` auto-registers unknown directories.
2. Add `up` with `--docker` / `--listen`, automatic daemon start, runtime start, and concise endpoint output.
3. Add `down` by path/ID without requiring manual Workspace ID.
4. Add `ps` merging persisted Workspace registry with observed daemon runtime list; support daemon-stopped state.
5. Add `ctl start|status|stop|restart|shutdown`; shutdown explicitly clears desired runtimes before daemon stop.
6. Add CLI process-level tests for one-shot up, path reuse/down, ps, restart and shutdown semantics.
7. Run fmt/test/vet/build and real binary smoke acceptance; update v0.3.1 planning to complete and stop before v0.4.
